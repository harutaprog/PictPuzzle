using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SavetestButtons : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        StageFlags stageFlags = Resources.Load<StageFlags>("FlagData");
//        stageFlags.FlagSet(stageFlags);
    }
    
}
